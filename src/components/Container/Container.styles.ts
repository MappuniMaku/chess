import { createUseStyles } from "react-jss";

export default createUseStyles({
  root: {
    display: "flow-root",
    boxSizing: "content-box",
    maxWidth: 1440,
    margin: [0, "auto"],
    padding: [0, 40],
  },
});
